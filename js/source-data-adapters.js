/**
 * BDFA Source Data Adapters
 *
 * Adapters will eventually normalize provider data, manual user input, and
 * screen-specific data into the Unified Financial Model.
 *
 * This module is intentionally a placeholder only. It contains documentation
 * and exported function stubs without implementation.
 *
 * Current rules for this adapter layer:
 * - No calculations.
 * - No mock data.
 * - No DOM access.
 * - No imports.
 * - No application wiring.
 * - No provider, backend, authentication, API, Plaid, or bank integration logic.
 */

/**
 * Adapt current dashboard-shaped data into the Unified Financial Model.
 *
 * @param {object} dashboardData - Future dashboard data input.
 * @returns {undefined} Placeholder only.
 */
export function adaptDashboardDataToUnifiedModel(dashboardData) {
  void dashboardData;
}

/**
 * Adapt manually entered account data into the Unified Financial Model.
 *
 * @param {Array<object>} accounts - Future manual account data input.
 * @returns {undefined} Placeholder only.
 */
export function adaptManualAccountsToUnifiedModel(accounts) {
  void accounts;
}

/**
 * Adapt manually entered bill data into the Unified Financial Model.
 *
 * @param {Array<object>} bills - Future manual bill data input.
 * @returns {undefined} Placeholder only.
 */
export function adaptManualBillsToUnifiedModel(bills) {
  void bills;
}

/**
 * Adapt manually entered income data into the Unified Financial Model.
 *
 * @param {Array<object>} incomeItems - Future manual income data input.
 * @returns {undefined} Placeholder only.
 */
export function adaptManualIncomeToUnifiedModel(incomeItems) {
  void incomeItems;
}
